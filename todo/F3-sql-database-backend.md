# F3. No SQL/database backend

**Type:** Feature
**Priority:** High

Memory and filesystem are dev/test backends. Production use needs a real backend.
This is where the library's value gets proven or disproven.
