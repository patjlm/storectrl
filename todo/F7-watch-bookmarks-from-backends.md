# F7. No watch bookmarks from backends

**Type:** Feature
**Priority:** Medium

`EnableWatchBookmarks` option exists but neither backend emits them. Only
`StoreListerWatcher` synthesizes bookmarks for the watchList protocol.
